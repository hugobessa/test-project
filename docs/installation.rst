============
Installation
============

At the command line::

    $ easy_install test-project

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv test-project
    $ pip install test-project
